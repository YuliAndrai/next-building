/**
 * @file apps/web/src/app/[locale]/eventos/page.tsx
 * @layer Presentation Layer / Page Component
 * @description Localized Events & Tour Dates route for ANDHRAY.
 */

import React from "react";
import EventosPage from "@/app/eventos/page";

export default function LocalizedEventosPage(): React.JSX.Element {
  return <EventosPage />;
}
