/**
 * @file apps/web/src/app/[locale]/live/page.tsx
 * @layer Presentation Layer / Page Component
 * @description Localized Live stream and broadcast route for ANDHRAY.
 */

import React from "react";
import LivePage from "@/app/live/page";

export default function LocalizedLivePage(): React.JSX.Element {
  return <LivePage />;
}
